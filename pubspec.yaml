name: banned_setstate_lint
description: A starting point for Dart libraries or applications.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.7.0

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0
  analyzer: ^7.4.5
  analyzer_plugin:
  custom_lint_builder: ^0.7.5

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
